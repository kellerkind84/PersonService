spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/person_service?serverTimezone=UTC&useSSL=false"
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
server:
  port: 8080
  servlet:
    context-path: /PersonService